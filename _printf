#include "main.h"

int _printf(const char *format, ...)
{
	int count, i;
	va_list list;
	if (format == NULL)
		return(-1);
	va_start(list, format);
	for (i = 0; *format; i++)
	{
		if (*(format + i) != '%')
		{
			write(1, format + i, 1);
			count++;
		}
		else
		{
			struct specifiers s = 
		}
	}	
	va_end(list);
}
